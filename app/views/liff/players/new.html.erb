<h2>新增玩家<h2>
<%= form_tag(liff_players_path) do %>
  名稱:<br>
  <input type="text" name="name"><br>
  暱稱:<br>
  <input type="text" name="nickname"><br>
  <input type="hidden" name="source_id" value="<%= params[:source_id] %>">
  <%= submit_tag 'go', data: { disable_with: 'go' } %>
<% end %>

<%= render(partial: 'liff/init', locals: { liff_id: Setting.liff_ids.player_new }) %>