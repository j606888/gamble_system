
    <!-- Sidebar -->
    <div class="bg-light border-right" id="sidebar-wrapper">
      <div class="sidebar-heading">麻將小幫手 V2.0</div>
      <div class="list-group list-group-flush">
      <% if user_signed_in? %>
        <div class="list-group-item list-group-item-action bg-light">Hi, <%= current_user.email %></div>
        <%= link_to '+', rooms_path, class:"list-group-item list-group-item-action bg-light" %>
        <% @join_rooms.each do |room| %>
         <%= link_to room.name, room_path(room), class: "list-group-item list-group-item-action bg-light" %>
        <% end %>
        <%= link_to '編輯用戶', edit_user_registration_path, class:"list-group-item list-group-item-action bg-light" %>
      <% else %>
        <%= link_to '登入', new_user_session_path, class:"list-group-item list-group-item-action bg-light" %>
      <% end %>
      </div>

      <% if user_signed_in? %>
        <%= link_to '登出', destroy_user_session_path, method: 'delete', class:"list-group-item list-group-item-action bg-light" %>
      <% end %>
    </div>

  <!-- Menu Toggle Script -->
  <script>
    $("#menu-toggle").click(function(e) {
      e.preventDefault();
      $("#wrapper").toggleClass("toggled");
    });
  </script>
